"use client";
import {useEffect,useState} from "react";import Link from "next/link";import {getApiClient} from "@/lib/api";import {useAuth} from "@/context/AuthContext";import type {ApiKeyInfo} from "@/lib/types";
const mask=(k:string)=>k.length>12?`${k.slice(0,8)}••••••••${k.slice(-4)}`:"••••••••";
export default function ApiKey(){const {plan}=useAuth(),[key,setKey]=useState<ApiKeyInfo|null|undefined>(undefined),[show,setShow]=useState(false),[confirm,setConfirm]=useState(false),[busy,setBusy]=useState(false),[notice,setNotice]=useState("");useEffect(()=>{getApiClient().getApiKey().then(setKey).catch(()=>setKey(null))},[]);
 async function copy(){if(!key?.secretAvailable){setNotice("Bản key đã che không thể sử dụng. Hãy tạo lại key để nhận secret mới.");return}try{await navigator.clipboard.writeText(key.key);setNotice("Đã sao chép secret key vào clipboard")}catch{setNotice("Không thể sao chép tự động")}}
 async function rotate(){setBusy(true);try{const next=await getApiClient().rotateApiKey();setKey(next);setShow(true);setNotice("Key mới chỉ hiển thị lần này. Hãy sao chép và lưu ngay; key cũ đã mất hiệu lực.") }catch{setNotice("Không thể tạo lại key") }finally{setBusy(false);setConfirm(false)}}
 return <div className="account-page"><header><span>DEVELOPER / CREDENTIALS</span><h1>API & MCP key</h1><p>Kết nối Prewise với AI agent, automation và ứng dụng nội bộ.</p></header>{plan?.tier!=="team"&&<div className="account-callout"><span>TEAM FEATURE</span><p>Key bên dưới phục vụ trải nghiệm demo. Nâng cấp Team để sử dụng endpoint production.</p><Link href="/pricing">Xem gói Team →</Link></div>}{key===undefined?<div className="key-skeleton">Đang thiết lập kênh bảo mật…</div>:!key?<div className="key-skeleton">Không thể tải API key. Hãy tải lại trang hoặc thử lại sau.</div>:<section className="key-panel"><div className="key-meta"><span>{key.secretAvailable?"SECRET KEY · CHỈ HIỂN THỊ MỘT LẦN":"KEY ĐÃ ĐƯỢC CHE"}</span><small>CREATED · {key.createdAt}</small></div><code>{key.secretAvailable?(show?key.key:mask(key.key)):key.key}</code><div className="account-actions">{key.secretAvailable&&<button className="secondary" onClick={()=>setShow(v=>!v)}>{show?"Ẩn key":"Hiện key"}</button>}<button className="secondary" disabled={!key.secretAvailable} onClick={copy}>{key.secretAvailable?"Sao chép secret":"Không thể sao chép bản che"}</button>{confirm?<><button className="danger" disabled={busy} onClick={rotate}>{busy?"Đang tạo…":"Xác nhận tạo lại"}</button><button className="secondary" onClick={()=>setConfirm(false)}>Hủy</button></>:<button onClick={()=>setConfirm(true)}>Tạo lại key →</button>}</div>{!key.secretAvailable&&<p className="key-warning">Vì lý do bảo mật, server không lưu secret gốc. Chuỗi có **** chỉ giúp nhận diện key và không dùng để xác thực.</p>}{confirm&&<p className="key-warning">Key hiện tại sẽ mất hiệu lực ngay lập tức.</p>}{notice&&<p className="form-success">✓ {notice}</p>}</section>}<p className="key-warning">Muốn lấy secret đầy đủ, chọn “Tạo lại key”, xác nhận rồi sao chép ngay khi secret mới xuất hiện. Secret đầy đủ chỉ được trả về một lần.</p><section className="integration-note"><span>MCP ENDPOINT</span><code>https://api.prewise.site/mcp</code><p>Không đưa key vào mã nguồn hoặc chia sẻ qua tin nhắn. Sử dụng biến môi trường cho mọi tích hợp.</p><Link className="mcp-quick-link" href="/account/mcp-connect">Kết nối nhanh với ChatGPT, Claude, Grok và các AI khác →</Link></section></div>}
