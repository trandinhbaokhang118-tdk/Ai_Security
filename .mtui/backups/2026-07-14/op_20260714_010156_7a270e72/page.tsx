"use client";

import Link from "next/link";
import { Suspense, useEffect, useState } from "react";
import { useParams, useSearchParams } from "next/navigation";
import { PrewiseShell, RiskDial, demoFindings } from "@/components/PrewiseUI";

type Finding = { title: string; detail: string; evidence: string; severity: "high" | "medium" };
type StoredRecord = { score?: number; content?: string; result?: { risk_score?: number; confidence?: number; analysis_time_ms?: number; threat_level?: string; evidence?: Array<{ source?: string; feature?: string; message?: string; severity?: string }> } };

function ResultContent() {
  const query = useSearchParams(); const id = query.get("id"); const isDemo = query.get("demo") !== "0";
  const fallbackScore = Number(query.get("score") || 87); const type = query.get("type") || "url";
  const [record, setRecord] = useState<StoredRecord | null>(null);
  useEffect(() => { if (!id) return; try { const raw = localStorage.getItem(`prewise-result:${id}`); if (raw) setRecord(JSON.parse(raw) as StoredRecord); } catch { /* Result query fallback remains available. */ } }, [id]);
  const score = record?.result?.risk_score != null ? Math.round(record.result.risk_score * 100) : record?.score ?? fallbackScore;
  const evidence = record?.result?.evidence ?? [];
  const findings: Finding[] = evidence.length ? evidence.slice(0, 6).map((item) => ({ title: item.feature || item.source || "Tín hiệu rủi ro", detail: item.message || "Backend phát hiện tín hiệu cần xem xét.", evidence: item.source || "url-risk-core", severity: item.severity === "critical" || item.severity === "high" ? "high" : "medium" })) : demoFindings;
  const confidence = record?.result?.confidence != null ? `${Math.round(record.result.confidence * 100)}%` : isDemo ? "92%" : "Đang xác định";
  const verdict = score >= 70 ? "Không truy cập hoặc cung cấp thông tin." : score >= 40 ? "Hãy xác minh thêm trước khi tiếp tục." : "Chưa thấy tín hiệu rủi ro nổi bật.";
  const description = score >= 70 ? "Trang đích có tín hiệu rủi ro cao. Không nhập mật khẩu, OTP, dữ liệu thẻ hoặc thông tin định danh." : score >= 40 ? "Có một số tín hiệu cần xác minh qua kênh chính thức trước khi thao tác." : "Kết quả hiện tại không thay thế việc xác minh tên miền và kênh liên hệ chính thức.";
  return <PrewiseShell><main id="main-content" className="report"><div className="report-title"><div><p className="eyebrow"><i /> ANALYSIS REPORT · {type.toUpperCase()}</p><span className="demo-badge">{isDemo ? "KẾT QUẢ MINH HỌA" : "KẾT QUẢ BACKEND"}</span><h1>Đánh giá rủi ro hoàn tất</h1><p>{isDemo ? "Prewise đã phát hiện nhiều tín hiệu cần được xem xét trước khi bạn tiếp tục." : "Kết quả dưới đây được trả trực tiếp từ Security Gateway."}</p></div><div className="report-actions" aria-label="Tác vụ báo cáo"><button type="button" disabled title="Chưa khả dụng">↓ Xuất báo cáo</button><button type="button" disabled title="Chưa khả dụng">↗ Chia sẻ</button></div></div><section className="report-overview" aria-label="Tổng quan kết quả"><RiskDial score={score} /><div className="verdict"><span>KHUYẾN NGHỊ</span><h2>{verdict}</h2><p>{description}</p><div><b>Độ tin cậy <span className="info-tip" tabIndex={0} role="note" aria-label="Giải thích độ tin cậy">?<span>Độ tin cậy được backend cung cấp khi có đủ tín hiệu; đây không phải là kết luận tuyệt đối.</span></span></b><strong>{confidence}</strong><i aria-hidden><em /></i></div></div><div className="signal-visual" aria-hidden><div className="report-core" /><small>{findings.length} SIGNALS CORRELATED</small></div></section><details className="result-guide"><summary>Cách đọc kết quả</summary><p><b>Risk score</b> là mức tổng hợp tín hiệu rủi ro trên thang 100. <b>Confidence</b> thể hiện mức nhất quán của bằng chứng. Luôn xác minh bằng kênh chính thức.</p></details>{isDemo ? <p className="demo-disclosure" role="status">ⓘ Đây là luồng demo dùng điểm số và bằng chứng cố định để minh họa giao diện.</p> : <p className="demo-disclosure" role="status">ⓘ Kết quả này là phản hồi backend thật. Latency: {record?.result?.analysis_time_ms ?? "—"} ms.</p>}<div className="report-grid"><section aria-labelledby="evidence-heading"><div className="section-label"><span id="evidence-heading">BẰNG CHỨNG</span><b>{findings.length} tín hiệu</b></div><div className="findings">{findings.map((finding, index) => <article key={`${finding.title}-${index}`}><span className={`severity ${finding.severity}`} aria-label={`Tín hiệu ${index + 1}`}>{String(index + 1).padStart(2, "0")}</span><div><h3>{finding.title}</h3><p>{finding.detail}</p><code>{finding.evidence}</code></div><em>{finding.severity === "high" ? "CAO" : "VỪA"}</em></article>)}</div></section><aside className="action-plan" aria-labelledby="actions-heading"><div className="section-label"><span id="actions-heading">HÀNH ĐỘNG ĐỀ XUẤT</span></div><ol><li><b>Không mở liên kết hoặc biểu mẫu</b><p>Đóng trang nếu bạn đã truy cập.</p></li><li><b>Không chia sẻ thông tin</b><p>Không nhập mật khẩu, OTP hay dữ liệu thẻ.</p></li><li><b>Xác minh qua kênh chính thức</b><p>Tự nhập tên miền hoặc gọi số đã xác thực.</p></li><li><b>Báo cáo tín hiệu đáng ngờ</b><p>Giúp cộng đồng nhận diện sớm hơn.</p></li></ol><button type="button" disabled title="Chưa khả dụng">Báo cáo website ↗</button></aside></div><div className="report-foot"><Link href="/analyze">← Phân tích nội dung khác</Link><span>Kết quả hỗ trợ quyết định, không thay thế xác minh hoặc đánh giá chuyên môn.</span></div></main></PrewiseShell>;
}
export default function ResultPage() { return <Suspense fallback={<PrewiseShell><main id="main-content" className="analysis-loading" aria-busy="true"><p>Đang chuẩn bị báo cáo…</p></main></PrewiseShell>}><ResultContent /></Suspense>; }
