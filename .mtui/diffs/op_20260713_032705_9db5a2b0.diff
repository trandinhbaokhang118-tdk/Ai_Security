@@ -5,6 +5,6 @@
 
 type Mode="url"|"email"|"sms";
 const samples={url:"https://secure-paypaI-support.com/account/verify",email:"KHẨN CẤP: Tài khoản của bạn sẽ bị khóa. Hãy xác minh mật khẩu và mã OTP trong vòng 30 phút tại liên kết bên dưới.",sms:"[NGAN HANG] Giao dịch 12.500.000đ đang chờ. Bấm link để hủy ngay và nhập mã OTP."};
-export default function AnalyzePage(){const [mode,setMode]=useState<Mode>("url"),[text,setText]=useState(""),[depth,setDepth]=useState("balanced"),[loading,setLoading]=useState(false);const router=useRouter();
+export default function AnalyzePage(){const query=useSearchParams(),[mode,setMode]=useState<Mode>("url"),[text,setText]=useState(""),[depth,setDepth]=useState("balanced"),[loading,setLoading]=useState(false);const router=useRouter();useEffect(()=>{const signal=query.get("signal");if(signal){setText(signal);if(!/^https?:\/\//i.test(signal)&&!/^www\./i.test(signal))setMode(signal.length<=180?"sms":"email")}},[query]);
  function analyze(){if(!text.trim())return;setLoading(true);const score=mode==="url"?87:mode==="email"?78:72;const record={id:Date.now().toString(),type:mode,content:text,score,date:new Date().toISOString(),findings:demoFindings};try{const old=JSON.parse(localStorage.getItem("prewise-history")||"[]");localStorage.setItem("prewise-history",JSON.stringify([record,...old].slice(0,20)))}catch{}setTimeout(()=>router.push(`/result/${record.id}?score=${score}&type=${mode}`),1250)}
  return <PrewiseShell><main className="workspace"><aside className="work-rail"><button className="new-scan">＋</button><span className="rail-line"/><button title="URL">↗</button><button title="Email">✉</button><button title="SMS">▤</button><div className="rail-bottom">⌘</div></aside><section className="editor-pane"><div className="pane-head"><div><span className="crumb">WORKSPACE / NEW ANALYSIS</span><h1>Phân tích tín hiệu</h1></div><span className="saved">● Lưu cục bộ</span></div><div className="mode-tabs">{(["url","email","sms"] as Mode[]).map(m=><button onClick={()=>{setMode(m);setText("")}} className={mode===m?"active":""} key={m}>{m==="url"?"Website / URL":m==="email"?"Email":"SMS / Tin nhắn"}</button>)}</div><div className="editor-area"><div className="line-nos">01<br/>02<br/>03<br/>04<br/>05<br/>06</div><textarea autoFocus value={text} onChange={e=>setText(e.target.value)} placeholder={mode==="url"?"Dán địa chỉ website cần kiểm tra…":"Dán toàn bộ nội dung cần kiểm tra…"}/><button className="sample" onClick={()=>setText(samples[mode])}>Dùng dữ liệu mẫu ↗</button></div><div className="editor-status"><span>{text.length} ký tự</span><span>UTF-8 &nbsp; • &nbsp; Tiếng Việt / English</span></div></section><aside className="inspector"><div className="inspector-head"><span>CẤU HÌNH ĐÁNH GIÁ</span><b>⌁</b></div><div className="config-group"><label>Phạm vi phân tích</label><div className="check-row"><span><i>✓</i>Dấu hiệu lừa đảo</span><em>Luôn bật</em></div><div className="check-row"><span><i>✓</i>Độ tin cậy & giả mạo</span></div><div className="check-row"><span><i>✓</i>Dữ liệu nhạy cảm</span></div><div className="check-row"><span><i>✓</i>Thao túng & thúc ép</span></div></div><div className="config-group"><label>Mức độ phân tích</label><div className="segments">{[["quick","Nhanh"],["balanced","Cân bằng"],["deep","Chuyên sâu"]].map(x=><button className={depth===x[0]?"active":""} onClick={()=>setDepth(x[0])} key={x[0]}>{x[1]}</button>)}</div><p>Cân bằng tốc độ, ngữ cảnh và độ chi tiết của bằng chứng.</p></div><div className="privacy-note"><b>◉ Xử lý có kiểm soát</b><p>Nội dung chỉ được gửi khi bạn chủ động phân tích. Không nhập mật khẩu, OTP hoặc khóa bí mật.</p></div><div className="analyze-action"><button disabled={!text.trim()||loading} onClick={analyze}>{loading?<><span className="spinner"/>Đang liên kết tín hiệu…</>:<>Phân tích nội dung <span>→</span></>}</button><small>⌘ + Enter</small></div></aside></main></PrewiseShell>}
