@@ -66,6 +66,10 @@
 }
 
 
+class ProviderRequestError(RuntimeError):
+    """A safe, user-displayable external provider failure."""
+
+
 def _entropy(data: bytes) -> float:
     if not data:
         return 0.0
