@@ -34,11 +34,14 @@
     anonymous_daily_scan_limit: int = 50
     max_upload_bytes: int = 10 * 1024 * 1024
 
-    # Payment credits (SePay bank-transfer webhook).
+    # SePay QR / webhook. QR is rendered through the SePay QR image endpoint;
+    # only public receiving-account details are returned to browsers.
     sepay_webhook_api_key: str = ""
     sepay_bank_account: str = ""
     sepay_bank_name: str = ""
     sepay_account_name: str = ""
+    sepay_qr_base_url: str = "https://qr.sepay.vn/img"
+    sepay_payment_expiry_minutes: int = 30
     sandbox_credit_price_vnd: int = 15000
     sandbox_session_minutes: int = 10
 
