@@ -102,7 +102,40 @@
         pred = self.engine.predict_url(url)
         score = self._finalize_score(pred.risk_score, pred.evidence)
         decision = self.policy.evaluate_human(score)
-        return self._build(score, decision, pred.evidence, Modality.URL, pred.model_version, t0)
+        response = self._build(score, decision, pred.evidence, Modality.URL, pred.model_version, t0)
+        keys = normalize_url(url)
+        severity_map = {"critical": 1.0, "high": .85, "medium": .6, "low": .35, "info": 0.0}
+        criterion_map = {"punycode_domain": 6, "homoglyph": 6, "brand_domain_mismatch": 5, "brand_typosquatting": 5, "deceptive_subdomain": 7, "no_https": 8, "is_shortlink": 17, "redirect_parameter": 18, "nested_url_redirect": 16, "credential_theft_intent": 29, "credential_lure_cluster": 29, "url_obfuscation": 18, "at_symbol": 18, "ip_host": 13, "risky_tld": 12, "high_entropy_domain": 6}
+        v2_evidence = []
+        for index, item in enumerate(pred.evidence):
+            cid = criterion_map.get(item.feature or "")
+            if not cid or item.contribution is None or item.contribution <= 0:
+                continue
+            quality = .8 if item.source == "url_risk_core" else .5
+            finding = item.feature or "unknown"
+            v2_evidence.append(EvidenceV2(
+                evidence_id=f"url-{index}-{uuid.uuid5(uuid.NAMESPACE_URL, keys.normalized_url + finding)}",
+                exact_subject_key=keys.exact_subject_key,
+                campaign_subject_key=keys.campaign_subject_key,
+                finding_key=make_finding_key(keys.exact_subject_key, finding, item.source),
+                incident_key=make_incident_key(keys.campaign_subject_key, finding, "scan"),
+                criterion_id=cid, source_id="internal_url", organization_id="internal",
+                source_family="strong_identity", matched_subject=MatchedSubject.EXACT_URL,
+                finding_type=finding, status=CriterionStatus.MALICIOUS if item.severity.value in {"critical", "high"} else CriterionStatus.SUSPICIOUS,
+                provider_verdict=ProviderVerdict.MALICIOUS if item.severity.value in {"critical", "high"} else ProviderVerdict.SUSPICIOUS,
+                severity=severity_map[item.severity.value], evidence_quality=quality, match_strength=1.0,
+                authority_tier=3, observed_at="scan", metadata={"summary": item.message},
+            ))
+        risk = assess_risk_v2(v2_evidence)
+        policy = PolicyEngineV2().decide(risk)
+        trace = asdict(risk)
+        trace.update({"schema_version": "2", "scoring_version": risk.scan_version, "raw_score": risk.base_risk_score, "final_score": risk.risk_score, "confidence": risk.confidence_score, "verdict": risk.risk_level, "decision": policy.decision.value, "next_action": policy.next_action.value})
+        response.risk_core = RiskCoreTrace.model_validate(trace)
+        response.schema_version = "2"
+        response.scoring_version = risk.scan_version
+        response.raw_score = round(risk.base_risk_score / 100, 4)
+        response.final_score = round(risk.risk_score / 100, 4)
+        return response
 
     # ------------------------------------------------------------------ text
     def assess_text(self, text: str, modality: str = "email", metadata: dict | None = None):
