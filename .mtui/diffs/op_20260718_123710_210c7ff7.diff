@@ -54,6 +54,8 @@
     "themida",
 )
 _DATA_ID_PATTERN = re.compile(r"[A-Za-z0-9._~=-]{1,256}")
+_SHA256_PATTERN = re.compile(r"[0-9a-fA-F]{64}")
+_MAX_PE_SECTIONS = 96
 _BENIGN_SCAN_LABELS = {
     "",
     "clean",
