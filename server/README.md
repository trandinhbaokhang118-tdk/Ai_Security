# Server Runtime Assets

Thư mục `server/` chứa các tài nguyên runtime phục vụ backend server.

Hiện tại:

- `models/`: các model AI dạng ONNX và metadata đi kèm.

Code backend vẫn nằm trong `backend/`; thư mục này chỉ dùng cho tài nguyên chạy server để
tách rõ khỏi code AI/training trong `ai/`.
